services:
  hoard:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - path: .env
        required: false
    volumes:
      - ${HOARD_DATA:-./data}:/app/data
    restart: unless-stopped
